@import "compass/css3/transform";
@import "compass/utilities/text/ellipsis";
@import "compass/css3/box";
@import "compass/utilities/lists/inline-list";
@import "mixins";


input#prezzo_con_sconto {
  margin-top: 8px;
}

input[type=number] {
  text-align: right;
}

.nav-fattura,
.nav-appunto,
.nav-cliente {
  @include inline-list;
}

.select-check {
  position: absolute;
  right: 12px;
  bottom: 3px;
}

.destinatario {
  font-size: 12px;
  color: #999;
}

.icon-status {
  margin: 5px;
  width: 22px;
  height: 22px;
  text-indent: -9999px;
  cursor: pointer;
  &.da_fare {
    background: url(<%= asset_path "task-sprite.png" %>) no-repeat scroll -26px -2px transparent;
  }
  &.preparato {
    background: url(<%= asset_path "task-sprite.png" %>) no-repeat scroll -26px -26px transparent;
  }
  &.completato{
    background: url(<%= asset_path "task-sprite.png" %>) no-repeat scroll -2px -2px transparent;
  }
  &.in_sospeso {
    background: url(<%= asset_path "task-sprite.png" %>) no-repeat scroll -2px -26px transparent;
  }
}


.appunto.in_sospeso {
  footer .copie strong {
    color: #d71322;
  }
   footer .importo-totale strong{
    color: #d71322;
  }
}

// corregge quadratino orrendo grigio
.dropdown.open .dropdown-toggle {
  // background: white;
}


.appunto-status .status-container {
  margin-left:20px;
}
.appunto-status .status-container, #td_task_info .star-container {
  float:left;
}

#status_span {
  height: 64px;
  width: 64px;
}

.appunto-status .status-container .active {
  background-attachment:initial;
  background-clip:initial;
  background-color:transparent;
  background-image: url(<%= asset_path "task-active.png" %>);
  background-origin:initial;
  background-repeat:no-repeat no-repeat;
}
.appunto-status .status-container .done {
  background-attachment:initial;
  background-clip:initial;
  background-color:transparent;
  background-image: url(<%= asset_path "task-completed.png" %>);
  background-origin:initial;
  background-repeat:no-repeat no-repeat;
}







.note form textarea {
	font-size: 14px;
}


.group:before,
.group:after {
  content:"";
  display:table;
}


.group:after {
  clear:both;
}

.group {
  zoom:1;
}

.telefono:before { 
  content: "\2706"; 
  padding-right: 5px;
}
.telefono:after { 
  content: ""; 
  clear: both;
}

// a[href^="mailto:"]:before { 
//   content: "\2709";
//   padding-right: 5px;
// }


#appunto-small {
  margin-top: 16px;
  .condizioni label {
    display: inline;
  }
}

#appunto-show {
  // 
  .field {
    line-height: 27px;
  }
  .field .label {
    float: left;
    width: 160px;
    text-align: right;
    padding-right: 10px;
    display: block;
  }
  .label + div {
    font-size: 18px;
    margin-left: 170px;
  }
}


.appunto_flash {
  margin-top: 16px;
  width: 75%;
  
  .field label {
    float: left;
    width: 30%;
    text-align: right;
    margin-right: 10px;
    line-height: 27px;
    // padding-top: 2px;
  }
  .actions {
    padding-left: 30%;
    margin-left: 10px;
  }
  .field input, .field textarea {
    font-size: 14px;
  }
  .field select {
    // background: transparent;
    width: 214px;
    padding: 5px;
    font-size: 14px;
    border: 1px solid #ccc;
    height: 27px;
  }
}

