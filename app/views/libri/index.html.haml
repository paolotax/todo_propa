= new_button( new_libro_url )

- title "Libri"

.row
  = render :partial => 'libri_side'
  
  .content-main.span6
    %header.content-header
      .header-inner
        %h2 Libri
    
    = render :partial => 'shared/filters'

    #libri.list
      = render @libri

    .stream-loader
      = paginate @libri

  / .side-right.span3.only_desktop    
  /   .module.white
  /     = render 'forecast'
