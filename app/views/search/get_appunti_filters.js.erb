

$(".side-bar").html(("<%= escape_javascript(render :partial => 'shared/episode_views')  %><%= escape_javascript(render partial: "shared/search_options") %>"));

if ($('.filters').length) {
  $('.filters').replaceWith("<%= escape_javascript(render partial: "shared/filters") %>");
} else {
  $("<%= escape_javascript(render partial: "shared/filters") %>").insertAfter($('.content-list header'));
}



window.appunti_pager.reset();

$('#appunti').data('json-url', "<%= url_for params.merge(controller: 'appunti', action: 'index', format: 'json') %>");

