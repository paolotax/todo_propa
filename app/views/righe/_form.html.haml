#create-riga.form-inline
  / .validity-summary-container
  /   %ul
  - @vendibili = Libro.unscoped.vendibili.select('distinct settore').order(:settore)
  = select_tag 'new_libro', option_groups_from_collection_for_select(@vendibili, :bros, :settore, :id, :titolo), {:include_blank => true, :class => 'chzn-select', 'data-placeholder' => "Seleziona un titolo..."}
  = text_field_tag "new_quantita", nil, :class => 'input-mini', placeholder: "quantità"
  = text_field_tag "new_prezzo", nil,   :class => 'input-mini', placeholder: "prezzo"
  = text_field_tag "new_sconto", nil,   :class => 'input-mini', placeholder: "sconto"
  / .field_inline
  /   = label_tag "new_consegnato", "Cons."
  /   = check_box_tag "new_consegnato"
  / .field_inline
  /   = label_tag "new_pagato", "Pagato"
  /   = check_box_tag "new_pagato"
  = link_to '+', 'javascript:void(0)', { id: "add-riga", :class => 'btn btn-success btn-mini' }
