.filters
  %strong Filtri: 
  - if params[:search].present?
    %span.filter
      = params[:search]
      = link_to "x", params.merge(:controller => :appunti, :action => :index, :format => 'json').merge(:search => nil, :page => nil)
  - if params[:tipo].present?
    %span.filter
      = params[:tipo].humanize.downcase
      = link_to "x", params.merge(:controller => :appunti, :action => :index, :format => 'json').merge(:tipo => nil, :page => nil)
  - if params[:status].present?
    %span.filter
      = params[:status].humanize.downcase
      = link_to "x", params.merge(:controller => :appunti, :action => :index, :format => 'json').merge(:status => nil, :page => nil)
  - if params[:provincia].present?
    %span.filter
      = params[:provincia] 
      = link_to "x", params.merge(:controller => :appunti, :action => :index, :format => 'json').merge(:provincia => nil, :page => nil)
  - if params[:citta].present?
    %span.filter
      = params[:citta] 
      = link_to "x", params.merge(:controller => :appunti, :action => :index, :format => 'json').merge(:citta => nil, :page => nil)