- if params[:search].present? || params[:tag].present? || params[:tipo].present? || params[:status].present? || params[:provincia].present? ||  params[:comune].present?
  
  .filters
    - if params[:action] == "get_appunti_filters" || params[:controller] == 'appunti'
      - @controller = 'appunti'
    - elsif params[:action] == "get_clienti_filters" || params[:controller] == 'clienti'
      - @controller = 'clienti'

    .pull-right
      = link_to "x", "", class: "close", remote: true

    %strong Filtri: 
    - if params[:search].present?
      %span.filter
        = params[:search]
        = link_to "x", params.merge(:search => nil, :page => nil), remote: true
    - if params[:tag].present?
      %span.filter
        = params[:tag]
        = link_to "x", params.merge(:tag => nil, :page => nil), remote: true
    - if params[:tipo].present?
      %span.filter
        = params[:tipo].humanize.downcase
        = link_to "x", params.merge(:tipo => nil, :page => nil), remote: true
    - if params[:status].present?
      %span.filter
        = params[:status].humanize.downcase
        = link_to "x", params.merge(:status => nil, :page => nil), remote: true
    - if params[:provincia].present?
      %span.filter
        = params[:provincia] 
        = link_to "x", params.merge(:provincia => nil, :page => nil), remote: true
    - if params[:comune].present?
      %span.filter
        = params[:comune] 
        = link_to "x", params.merge(:comune => nil, :page => nil), remote: true