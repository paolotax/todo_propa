= form_tag url_for( controller: params[:controller], action: 'index'), method: "get", class: "navbar-search pull-right" do
  = text_field_tag :search, params[:search], class: "search-query", placeholder: "Cerca #{params[:controller].capitalize}"
  / = submit_tag "Cerca #{params[:controller].capitalize}", :name => nil