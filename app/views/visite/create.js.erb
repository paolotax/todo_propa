if ($('#appunti').length > 0) {
  <%- @visita.appunti.each do |a|  %>
    appunto = $("#<%= dom_id(a) %>")
    appunto.toggleClass('favorited');
    $('.baule_btn', appunto).html("<%= escape_javascript(render 'visite/scarica_baule', visita: @visita) %>")
  <% end %>  
};

if ($('.cliente').length > 0) {
  cliente = $("#<%= dom_id(@visita.cliente) %>")
  cliente.toggleClass('favorited');
  console.log(cliente);
  $('.baule_btn', cliente).replaceWith("<%= escape_javascript(render 'visite/scarica_baule', visita: @visita) %>")
};

$(".nav-baule sup").text("<%= current_user.visite.nel_baule.size %>");

// var new_navbar = $("<%= escape_javascript(render 'shared/header') %>")
// var new_toolbar = $(".toolbar-buttons.pull-right", new_navbar)
// $(".navbar.navbar-fixed-top .toolbar-buttons.pull-right").replaceWith(new_toolbar)
// 
// 
// var new_navbar = $("<%= escape_javascript(render 'shared/header') %>")
// var new_toolbar = $(".toolbar-buttons.pull-right", new_navbar)
// $(".navbar.navbar-fixed-top .toolbar-buttons.pull-right").replaceWith(new_toolbar)



